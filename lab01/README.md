App.js: display in the app

package.json: keep track of the versions of debugger and dependencies
node_modules: packages that is imported into the device
.gitignore: files that git should ignore

Seongrim Choi